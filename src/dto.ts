import { GameConfiguration } from "./game";
import { PlayerIdentity } from "./player";

export interface GameConfigurationDto extends GameConfiguration {};
export interface PlayerIdentityDto extends PlayerIdentity {};
